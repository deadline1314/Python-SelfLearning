from cx_Freeze import setup, Executable

setup(name='urlParse', version='0.1', description='Parse stuff', executables=[Executable("37. parsing websites with re and urllib.py")])

# in command line: python 44.cx freeze python to exe.py build
